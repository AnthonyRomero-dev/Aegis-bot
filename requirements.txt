# ─── Requeridas (el bot no arranca sin estas) ───────────────────────────────
discord.py>=2.3.2
aiohttp>=3.9.0
python-dotenv>=1.0.0
aiosqlite>=0.19.0
Pillow>=10.0.0
emoji>=2.8.0

# ─── Opcionales (mejoran el bot pero no son obligatorias) ────────────────────

# Normalización de texto con acentos en el filtro de palabras
Unidecode>=1.3.7

# Colores en los logs de consola
colorama>=0.4.6

# ─── Desarrollo / tests ──────────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.23.0
